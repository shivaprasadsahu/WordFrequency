Steps:
1) double click on the batch file (.bat) to start the application
2) different options will be displayed
3) select 1 to process a file. Only files with .txt extension are supported. Place this file under the files folder.
4) on request, provide the path for the file which contains the realtive path for the other files(Only files with .txt extension are supported). 
It is recommended to provide the absolute path of the file.
5) Form this point on follow the instructions as shown in the UI.